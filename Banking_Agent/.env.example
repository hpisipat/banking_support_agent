OPENAI_API_KEY = <OPENAI API KEY>

# Optional — enables real road distance in locator using Open Route Service API Key
# Remove comment to activate (requires internet access, not VPN)
# ORS_API_KEY=your-ors-api-key-here